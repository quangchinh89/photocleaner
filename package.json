{
  "name": "photoclean-ai-standalone",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}
